# 협업 가이드

## 시작 전

```bash
pnpm install --frozen-lockfile
cp .env.example .env.local
pnpm check
```

현재 발표용 fixture 흐름은 환경변수 없이 실행된다. `.env.local`의 키는 live adapter 작업 전까지 비워 두며 실제 자격증명은 Git, 이슈, 채팅, 화면 녹화와 로그에 남기지 않는다.

## 화면 구현 시작 조건

- 별도 GitHub 랜딩페이지 저장소에서는 시각 요소만 참고하며 구조와 기능은 제품 스펙을 따른다.
- 메인 웹사이트는 디자인 담당자의 확정본을 기준으로 구현한다.
- 임시 화면이나 이전 mock 화면을 시각 참고자료로 사용하지 않는다.
- 디자인 토큰, 실제 문구 길이, 모바일·오류·빈 상태를 받은 뒤 컴포넌트 경계를 확정한다.

## 역할과 파일 소유권

개발자 A 박지성은 제품 UI, 결정적 랜딩·캐러셀 렌더러, export, E2E와 배포를 맡는다. 개발자 B 홍성주는 `CampaignSpec`, OpenAI·Supabase adapter, 내부 API, 공개 route 데이터 wrapper와 단위 테스트를 맡는다.

공유 계약과 디자인 토큰은 한 명만 수정한다. 상대 소유 파일을 동시에 수정하지 않고 API 경계를 바꿀 때는 소비자와 테스트 영향을 함께 검토한다.

## 브랜치와 통합

- `main`은 검증 가능한 상태로 유지한다.
- 작업 브랜치는 `feat/<짧은-이름>`, `fix/<짧은-이름>`, `docs/<짧은-이름>` 형식을 권장한다.
- 작은 완료 단위로 자주 통합한다.
- 커밋 전 `pnpm check`를 실행한다.
- 구현이 생긴 뒤에는 production build와 가장 가까운 테스트를 `check`에 추가한다.
- 앱 엔트리를 추가하면 ESLint의 `@next/next/no-html-link-for-pages` 규칙을 다시 활성화한다.
- 발표는 사람이 직접 조작하며 자동 재생 기능을 만들지 않는다.

## PR 체크

- 변경 목적과 P0/P1 범위를 적었다.
- 실제 실행한 검증만 기록했다.
- 화면은 확정된 디자인과 제품 스펙을 기준으로 구현했다.
- 근거 없는 후기, 수치, 인증, 시장성 주장을 추가하지 않았다.
- 개인정보와 자격증명이 브라우저·로그·저장소에 노출되지 않는다.
- 긴 한글 문구, 375px 화면과 실패 상태를 확인했다.
