# Market Valley UI 작업 로그 A

- 작성일: 2026-08-26
- 작업 브랜치: `feat/main-project-ui-auth`
- 기준 디자인: Figma `유니톤`
- 작업 목적: 발표용 데모 플로우를 Figma 화면과 동일하게 구성하고, 실제 개발 흐름과 분리 가능한 목 로그인·목 리포트 데이터를 적용한다.

## 1. 메인 프로젝트 페이지

- 메인 페이지 전체 구성을 Figma 프로젝트 화면에 맞췄다.
- 상단 바는 Figma 구성을 유지하면서 로그인 버튼을 `Google로 로그인`으로 변경했다.
- 비로그인 사용자가 `광고 만들기`를 누르면 Google 로그인 화면으로 이동하도록 연결했다.
- Google 목 로그인 완료 후 메인으로 돌아오며 로그인된 상태로 표시되도록 구성했다.
- 전체 프로젝트 카드의 색상, 진행률, 남은 시간, 진행 바를 프로젝트별로 다르게 구성했다.
- 완료 프로젝트는 `검증 완료` 필터에서 표시되도록 연결했다.
- `마감한입` 프로젝트의 `10시간 남음` 표기를 `검증 완료`로 변경했다.
- `새 아이디어 검증하기` 박스와 Figma에 없는 하단 검정색 영역을 제거했다.

## 2. 온보딩

- Figma의 `2-1. 온보딩`, `2-2. 온보딩` 화면 흐름을 메인 페이지 다음 단계로 연결했다.
- `예시 불러오기` 버튼과 입력 영역의 좌우 정렬을 Figma 기준으로 조정했다.
- 임의로 추가했던 `AI가 랜딩·카드뉴스 문구를 생성해요` 문구를 제거했다.
- Figma에 없는 설명이나 UI 요소를 추가하지 않는 방향으로 정리했다.

## 3. 진행 상황 페이지

- 카드뉴스 생성, 랜딩 페이지 생성, 시장 데이터 수집, 검증 완료로 이어지는 진행 화면을 Figma의 네 개 프레임 흐름에 맞췄다.
- 발표용 데모에서는 실제 광고 게시와 시장 반응 대기 없이 다음 단계로 이동할 수 있도록 `다음 단계로` 버튼을 제공했다.
- 실제 개발에서는 Meta 광고 게시 후 시장 반응 데이터를 기다리는 흐름으로 교체해야 한다는 구분을 유지했다.

## 4. 검증 리포트

### 판정 로직과 목 지표

- CTR이 1% 미만이면 `부적합`, 1% 이상 3% 미만이면 `적합`, 3% 이상이면 `매우 적합`으로 분류한다.
- 발표용 목데이터는 `매우 적합` 결과가 표시되도록 구성했다.
- 주요 예시 지표는 노출 수 `1,800,820회`, CTR `12.6%`, 예약률 `4%`다.

### 산출물 미리보기

- 광고 카드뉴스 소재는 AI가 생성한 실제 카드뉴스 5장을 사용한다.
- 이전·다음 버튼과 스와이프로 한 장씩 이동하도록 구성했다.
- `1/5` 숫자 표기와 가로 스크롤바를 제거하고 Figma와 같은 원형 페이지 표시를 적용했다.
- 랜딩 페이지 이미지는 고정 목업 대신 AI가 생성한 실제 랜딩 페이지를 iframe으로 표시한다.

### 그래프와 반응형 UI

- 리포트 섹션, 카드, 퍼널, 막대그래프, 도넛, 사용자 행동 데이터에 진입 애니메이션을 적용했다.
- 성별 목데이터는 남성 185명, 여성 247명, 전체 432명으로 변경하고 도넛 비율에 연결했다.
- 연령대는 18–24세 16%, 25–34세 37%, 35–44세 29%, 45–54세 18%로 구성했다.
- 거주지는 서울 32%, 인천 13%, 경기 25%, 부산 10%, 대구 7%, 대전 5%, 광주 5%, 울산 3%로 구성했다.
- 거주지 그래프는 값이 커져도 카드 밖으로 넘치지 않으며, 작은 화면에서는 내부 가로 스크롤로 탐색할 수 있다.
- 모바일 연령대 라벨이 두 줄로 깨지지 않도록 열 너비를 보정했다.
- 노출 수 그래프는 Figma `Frame 23`의 384×230 크기와 포인트 위치를 기준으로 다시 작성했다.
- 노출 수 그래프의 세 원형 포인트는 `(156, 140)`, `(240, 159)`, `(309, 61)`에 배치했다.
- 각 포인트를 곡선의 최고점·최저점으로 사용하고 수평 접선의 연속 베지어 곡선으로 연결해 꺾임을 줄였다.

## 5. 검증 결과

- `components/campaign-report.tsx` ESLint 검사를 통과했다.
- 리포트 지표 관련 단위 테스트 7개를 통과했다.
- 1440px 데스크톱과 375px 모바일 화면을 로컬 Chromium으로 확인했다.
- 모바일 페이지 전체 가로 넘침이 없음을 확인했다.
- 노출 수 그래프의 카드 크기, 포인트 좌표와 표시 상태를 브라우저에서 확인했다.

## 6. 구현 시 유지할 구분

- 현재 Google 로그인은 발표용 목 로그인이다.
- 현재 시장 반응 수집 단계와 리포트 수치는 발표용 목데이터다.
- 실제 개발에서는 로그인 세션, Meta 광고 게시, 시장 반응 대기, 실측 CTR·예약률·인구통계 데이터 저장 흐름으로 교체해야 한다.
- 이후 UI 변경에서도 Figma에 없는 요소나 문구를 임의로 추가하지 않는다.

## 7. `main` 병합 충돌 해결

- `origin/main`의 배포·보안·AI 품질 변경을 `feat/main-project-ui-auth`에 병합했다.
- 충돌이 발생했던 `README.md`, `docs/demo-runbook.md`, `lib/ai/campaignPrompts.ts`의 내용을 정리하고 충돌 표식을 모두 제거했다.
- Windows에서 URL pathname이 `C:\\C:\\...` 형태로 계산되던 배포 테스트를 `fileURLToPath` 기반으로 수정했다.
- Git Bash 경로와 운영체제별 PATH 구분자를 반영했다.
- `bash`와 `jq`가 필요한 OCI 셸 통합 테스트는 Linux CI에서 유지하고 Windows에서는 명시적으로 건너뛰도록 했다.
- 병합 커밋: `d15f298 Merge main into feat/main-project-ui-auth`
- 병합 후 lint와 TypeScript 검사를 통과했다.
- 전체 단위 테스트 결과는 171개 통과, Windows 환경 의존 테스트 3개 건너뜀이다.
